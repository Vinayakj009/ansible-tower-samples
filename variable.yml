---
- name: "service module demo"
  hosts: all
  become: true
  tasks: 
    - name: Restart filebeat 
      service:
       name: "{{ services }}"
       state: restarted
       enabled: yes
    vars:
      services:
       - filebeat
